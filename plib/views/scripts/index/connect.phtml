<?php echo $this->renderTabs($this->tabs); ?>

<h2>O365 verknüpfen: <?php echo htmlspecialchars($this->domain->getName()); ?></h2>

<?php if (!$this->globalReady): ?>
    <div style="background:#fff3cd;border:1px solid #ffc107;border-radius:6px;padding:16px">
        &#9888; Bitte zuerst die globale Azure App Client ID in den
        <a href="<?php echo pm_Context::getActionUrl('index', 'settings'); ?>">Einstellungen</a> hinterlegen.
    </div>

<?php elseif (!empty($this->tenantId)): ?>
    <div style="background:#f0fff4;border:1px solid #68d391;border-radius:6px;padding:16px;margin-bottom:20px">
        &#10003; <strong>Verbunden</strong> &mdash; Tenant ID: <code><?php echo htmlspecialchars($this->tenantId); ?></code>
    </div>
    <a href="<?php echo htmlspecialchars($this->oauthStartUrl); ?>" class="btn">
        Berechtigungen erneuern
    </a>
    &nbsp;
    <a href="<?php echo pm_Context::getActionUrl('index', 'mailboxes') . '?domain_id=' . $this->domainId; ?>" class="btn btn-primary">
        Postfächer anzeigen &rarr;
    </a>

<?php else: ?>
    <p>Klicken Sie auf den Button und melden Sie sich als O365-Administrator an.<br>
    Alle benötigten Berechtigungen werden automatisch angefragt.</p>
    <a href="<?php echo htmlspecialchars($this->oauthStartUrl); ?>"
       class="btn btn-primary"
       style="display:inline-flex;align-items:center;gap:8px">
        <img src="https://learn.microsoft.com/favicon.ico" width="16" height="16" alt="">
        Mit Microsoft verbinden
    </a>
<?php endif; ?>
