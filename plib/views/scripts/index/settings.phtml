<?php echo $this->renderTabs($this->tabs); ?>

<h2>Einstellungen</h2>

<div style="background:#f0f4ff;border:1px solid #c0d0f0;border-radius:6px;padding:12px 16px;margin-bottom:20px">
    <strong>Azure AD Multi-Tenant App einrichten (einmalig für alle Kunden):</strong>
    <ol style="margin:8px 0 0;padding-left:20px">
        <li><a href="https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps/CreateApplicationBlade" target="_blank">App-Registrierung erstellen</a>
            &rarr; Typ: <strong>Konten in einem beliebigen Organisationsverzeichnis (Multimandant)</strong></li>
        <li>API-Berechtigungen &rarr; Microsoft Graph &rarr; Anwendungsberechtigungen:
            <ul style="margin:4px 0;padding-left:16px">
                <li><strong>User.Read.All</strong> – Benutzer/Postfächer auflisten</li>
                <li><strong>Mail.Read</strong> – E-Mails lesen und migrieren</li>
            </ul>
            &rarr; <strong>Admin-Zustimmung erteilen</strong> (für Ihren eigenen Tenant)</li>
        <li>Authentifizierung &rarr; Web &rarr; Redirect URI: <code><?php echo htmlspecialchars($this->callbackUrl); ?></code></li>
        <li>Client ID + Secret unten eintragen</li>
    </ol>
    <p style="margin:8px 0 0;font-size:0.9em;color:#555">
        Migration erfolgt über die Microsoft Graph API &ndash; kein IMAP-Zugriff nötig.
        E-Mails werden via <code>doveadm save</code> direkt in Dovecot importiert (keine Duplikate).
    </p>
</div>

<?php echo $this->form; ?>
